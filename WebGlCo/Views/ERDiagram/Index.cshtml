@{
    ViewBag.Title = "ERDiagram";
}
@Scripts.Render("~/bundles/three")
@Scripts.Render("~/bundles/erDiagram")

<div class="row">
    <div class="col-md-4">
        <div>
            <div id="canvas-frame" style="width:800px; height:550px;"></div>
            <br>
            <input class="btn btn-primary" type="button" value="LayerA" onclick="focusLayerA();" />
            <input class="btn btn-primary" type="button" value="LayerB" onclick="focusLayerB();" />
            <div>
                <input id="translucenceCheck" type="checkbox" onclick="makeTranslucence(this.checked);" />
                <label>半透明表示</label>
            </div>
       </div>
    </div>
</div>
